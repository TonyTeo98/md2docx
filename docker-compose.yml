version: '3.8'

services:
  # 前端应用
  web:
    build: .
    ports:
      - "80:80"
    restart: unless-stopped

  # 协作服务器 (可选)
  collab:
    build:
      context: ./server
      dockerfile: Dockerfile
    ports:
      - "1234:1234"
    restart: unless-stopped
